<div class="list-group-item">

  <h4 class='list-group-item-heading'><%= item.name %> <%= link_to "", "/users/#{current_user.id}/items/#{item.id}", method: :delete, class: 'glyphicon glyphicon-remove' %></h4>
  <h5>Time remaining: <%= distance_of_time_in_words(item.created_at + 7.days, Time.now) %></h2>
</div>
